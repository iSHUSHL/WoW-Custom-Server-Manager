name: PlayerBots All Configs + Clean Titlebar v1595

on:
  push:
    paths:
      - '.github/workflows/playerbots-allconfigs-titlebar-v1595.yml'

permissions:
  contents: write

jobs:
  patch-build-package:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Apply 1.5.95 fix
        run: python3 Scripts/apply-v1595-playerbots-allconfigs-titlebar.py
      - name: Remove temporary patch files
        run: |
          rm -f Scripts/apply-v1595-playerbots-allconfigs-titlebar.py
          rm -f .github/workflows/playerbots-allconfigs-titlebar-v1595.yml
      - name: Validate scripts and app
        run: |
          bash -n Build.command
          find Scripts -type f -name '*.sh' -print0 | xargs -0 -n1 bash -n
          swift build -c release
      - name: Commit validated changes
        run: |
          git config user.name 'WoWCC Builder'
          git config user.email 'wowcc-builder@users.noreply.github.com'
          git add -A
          git commit -m 'Fix all WotLK PlayerBots config ports and clean titlebar (1.5.95)'
          git push origin main
      - name: Stage tracked source without nested ZIP
        run: |
          mkdir -p artifact/WoW-Custom-Server-Manager
          git archive --format=tar HEAD | tar -x -C artifact/WoW-Custom-Server-Manager
      - uses: actions/upload-artifact@v4
        with:
          name: WoWServerControlCenter-1.5.95-PlayerBotsAllConfigs-CleanTitlebar-ONE-ZIP
          path: artifact/WoW-Custom-Server-Manager
          if-no-files-found: error
